import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 24,
    justifyContent: 'center',
    alignItems: 'center',
  },
  sampleText: {
    textAlign: 'center',
    fontSize: 14,
    marginBottom: 16,
  },
  text: {
    textAlign: 'center',
    fontSize: 14,
    marginBottom: 16,
  },
});
